apiVersion: v1
kind: Service
metadata:
  name: {{ include "miportfolio.fullname" . }}
  labels:
    {{- include "miportfolio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "miportfolio.selectorLabels" . | nindent 4 }}
