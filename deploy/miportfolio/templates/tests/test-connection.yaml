apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "miportfolio.fullname" . }}-test-connection"
  labels:
    {{- include "miportfolio.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "miportfolio.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
